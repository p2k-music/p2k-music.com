---
name: p2k-site-project
description: Context for the p2k-music.ca artist website and the planned backend work
metadata: 
  node_type: memory
  type: project
  originSessionId: 300dbb52-76d8-454a-826a-b22a8d48c503
---

This repo (`C:\Users\p2k\Downloads\p2k-Aaron-WEBSITE-help`) is **P2K's** music artist site, which Aaron is helping refine. P2K is Aaron's friend; Aaron is doing the refining/audit.

**ARCHITECTURE (changed 2026-07-03 → now MULTI-PAGE):** was one big `index.html`; now split into 11 pages (`index.html`=Home, `about/music/podcast/videos/tour/gallery/merch/news/contact/admin.html`) that all share **`assets/styles.css`** + **`assets/app.js`** (the entire original CSS/JS, extracted). `app.js` was made page-safe (every render/listener guarded so each page only wires elements it has; `updateAdminUI` appended as a page-safe override). Nav active-state via `<body data-page="...">` + CSS. Each page has a fixed bottom **equalizer band** `#pageViz` (`startViz` retargeted from `#heroViz`). Admin panels (CMS/wallet/checkin/profit) live on `admin.html`; login modal auto-opens there. Pre-split single-file version backed up to `Downloads\p2k-backup-single-page-v1[.zip]`. Editing note: change shared behavior in `assets/app.js`/`assets/styles.css`, NOT per-page. All data (now **53 real tracks** — `DEFAULT_SONGS`) lives in `app.js`.

**Catalog + visuals (2026-07-04):** rebuilt `DEFAULT_SONGS` to the 53 real filenames P2K has (added *P2K EKA JENVA*, *…Top Of The World*, *p2k_freestyle_7*, *p2k_haupate^^*, etc.); Music page got a customer library bar (search/sort/count via `initLibrary`+`renderSongs` filter). **The real MP3s are NOT in the repo** — `audio/` holds 1-byte preview placeholders; the real files must be dropped in for deploy/backup (placeholder playback throws harmless "no supported source" errors locally). Added an animated **ribbon wallpaper** (`injectRibbons` + `#ribbonBg` in CSS, on every page) and a wordless **energy-swirl** canvas on the audio player (`drawSwirl`, activates on play via `setPlayingUI`).

**GitHub backup skill (2026-07-04):** `.claude/skills/backup-github/` (SKILL.md + backup.ps1 + backup.sh) — `git add -A` → timestamped commit → push to `origin`. Git repo IS initialized (branch `main`, initial commits made, `audio/` + `*.zip` excluded). **PUSHED 2026-07-06.** Remote `origin` = `https://github.com/p2k-music/p2k-music.com.git` (org `p2k-music`); **Git Credential Manager auth is cached so `git push` works non-interactively** (no `gh` CLI, still not installed). **IDENTITY: the repo belongs to P2K — commit as `p2k-music <tajallatajalla2@gmail.com>` (his GitHub account), NOT Aaron.** The local git config had wrongly overridden `user.email` to Aaron's `aaron.styles9393@gmail.com` (name "P2K Music"); fixed local config to `p2k-music`/`tajallatajalla2@gmail.com` and rewrote all history (filter-branch) + force-pushed so every commit is credited to P2K. Global config is already `p2k-music`/`tajallatajalla2`. Never author commits here with Aaron's email. See [[aaron-working-style]] (Aaron helps; P2K owns). The remote had one prior manual "Add files via upload" commit (`p2k-music-website.zip`) — merged it in via `--allow-unrelated-histories` (kept, now tracked despite the `*.zip` gitignore — could `git rm --cached` it if you want it gone). `main` tracks `origin/main` at `dd200d5`. Future backups: just run the backup-github skill (`git add -A` → timestamped commit → push). Also made a local snapshot zip `p2k-music-website-backup-<stamp>.zip` in the folder (234MB incl. tracks, excludes secrets).

On 2026-07-03 the entire front end was overhauled: kept P2K's cyan/coral neon + glassmorphism aesthetic but ramped visuals (particle bg, live Web-Audio visualizer, scroll-reveal, scrollspy, count-up stats, intro loader, 3D tilt) and added pro sections (About, Featured, Stats, Videos, Tour, Merch, booking form). Original saved to `index.original.backup.html`.

**Backend BUILT 2026-07-06** (`server/`, zero-dependency Node — built-in `http`/`crypto`/`node:sqlite`; Node 26 confirmed local). Runs via `node server/server.js` on :8123 and now serves the whole static site too (`.claude/launch.json` switched from `python -m http.server` to node). Files: `config.js` (env/.env loader, demo-vs-live), `db.js` (SQLite, money in INTEGER CENTS, `walletSummary`=sales+listen−payouts), `auth.js` (scrypt passcode, HMAC signed HttpOnly session+visitor cookies, CSRF double-submit, per-IP token-bucket rate limits), `http.js` (CSP + security headers, hardened static server with Range streaming + traversal guard), `paypal.js` (Orders v2 + Payouts v1, live via `PAYPAL_CLIENT_ID/SECRET` else DEMO simulation), `server.js` (router). Endpoints: `/api/admin/{login,logout,session}`, `/api/earnings`, `/api/withdraw`, `/api/listen-tick` (anti-fraud: signed visitor cookie + ≥55s cadence + per-visitor/-IP daily caps), `/api/orders`(+`/:id/capture`), `/api/ticket/:code`(+`/checkin`), `/api/tickets/issue`, `/api/revenue`, `/api/paypal/webhook`, and `/t/:code` (public ticket-status HTML page = the QR target). **Client (`app.js`) rewired** to these with graceful offline fallback: admin auth is now SERVER-verified (client passcode `[redacted]` REMOVED from JS — seed a real one via `P2K_ADMIN_PASSCODE` in `server/.env`; legacy default still seeds w/ loud warning), withdraw sends CSRF, song/ticket purchases go through create-order→capture (records server revenue; live mode verifies PayPal before unlock), and door check-in validates against the server so it works cross-device. **DEMO mode** (no PayPal creds) simulates payments/payouts so it's fully previewable. Verified live: 25/25 API test suite + in-browser auth/CSRF/balance chain all pass. See `SECURITY-AUDIT.md` (fixes V1 client passcode, V2 honor-system pay, V3 forgeable tickets, V4 unauth wallet, V5 tick fraud, V6 headers, V7 traversal) + `server/README.md`. **Still TODO for real money (from audit):** wire PayPal JS SDK/webhook for true live capture (R4), gate raw `audio/` file URLs (R2 — files are public static), server-side event/merch price catalog (R3), and resolve B1 (listen-earnings are a projection) + B2 (payout goes to the same `p2key1@gmail.com` that receives — confirm personal-vs-business). `server/.env` + `server/data/` are gitignored.

**Audio + background (2026-07-06):** the real MP3s were dropped into a new `Audio-Files/` folder; copied all 48 into `audio/` (overwrote the 1-byte placeholders, 259.8 MB). **5 catalog entries still have NO real file** (ids 4 `DEEZY ME !`, 32 `P2K EKA JENVA !`, 35 `p2k sec verse…Top Of The World`, 38 `p2k_freestyle_7`, 39 `p2k_haupate^^`) — their placeholders were deleted so `checkFileAvailability()` 404-hides them; Music page now shows **48 playable** tracks (verified: real track plays, readyState 4, ~179s). Drop those 5 real files into `audio/` (exact names) and they auto-reappear. **Background re-envisioned:** replaced the flat horizontal ribbon wallpaper with an animated **aurora-orb nebula** (`#auroraField`, 6 screen-blended drifting radial-gradient orbs in the cyan/violet/magenta/coral/gold palette + depth vignette + "breathing" base aurora) and upgraded `initParticles` to a multi-colour glowing constellation with cursor parallax. `Audio-Files/` is gitignored (raw upload dupe). NOTE: `preview_screenshot` times out in this env (continuous rAF animation + AdSense polling never reach idle) — verify visuals with `preview_inspect`/`eval` instead. **Background PHOTO added 2026-07-06:** P2K supplied a ribbon photo (`aromantic-asexual-4096x2304-11873.jpg`, gitignored) → downscaled+compressed to `background.jpg` (1920×1080, 454KB, via PowerShell System.Drawing) which every page already references on `#customBg`. Tuned `.custom-bg` to opacity 0.5 / z-index -2 (behind particles) + a `::after` dark gradient scrim for text readability, and dialed the aurora orbs to opacity 0.3 so the photo is the star. Opacity/scrim may need tuning per Aaron's eye (couldn't screenshot).

**File Converter integrated (2026-07-06):** Aaron built a "Universal File Converter" at `converter/index.html` (drag/drop, localStorage credit system — 100 free, 5/conversion; real image conversion via canvas, audio/video/doc simulated). Rewrote it in the P2K neon-glass aesthetic (loads `../assets/styles.css` + fonts/FA, hardcoded P2K nav/footer with `../` links + inline particle canvas + `#auroraField` orbs + `../background.jpg` — does NOT load shared app.js, so its converter classes are namespaced `conv-*` to avoid colliding with styles.css; kept all his JS verbatim except `format-btn`→`conv-format-btn`). Added `body[data-page="converter"]` to the nav-active CSS rule, and `injectConverterNav()` in app.js appends a "Converter" link to `#navLinks` (before Contact) + `.footer-nav` on every page → `converter/index.html`. Verified: renders, no errors, E2E file→format→convert flow works.

**Admin login → 2-account email-2FA (2026-07-06):** Replaced the single server passcode (old `[redacted]`/`[redacted]` is GONE) with TWO admin accounts — `tajallatajalla2@gmail.com` (P2K) + `aaron.styles9393@gmail.com` (Aaron) — each logs in with email+password, then a 6-digit one-time code. Flow: `POST /api/admin/login {email,password}` (scrypt verify + anti-enumeration dummy-hash) → creates a hashed, 5-min, attempt-capped code → `POST /api/admin/verify {challenge,code}` → issues the session (now carries the admin email). New: `server/email.js` (zero-dep SMTP-over-TLS sender + demo fallback), `admins`+`login_codes` tables, config `admins`/`smtp`/`codeTtl`. Client: `admin.html` login modal is 2-step; `app.js` `handleAdminLogin`+`handleAdminVerify`+`resetAdminLogin` (old `passcodeInput` gone). **HARDENED (2026-07-06, "nobody else logs in"):** the code now **fails closed** — it is only ever emailed; the on-screen fallback is gated behind `DEV_SHOW_CODE=1` AND localhost-only (`isLoopback`), so a remote user never sees it. With no SMTP + no dev flag, `POST /api/admin/login` returns **503 email_not_configured even for a CORRECT password** (verified live) → an attacker needs the password AND the email inbox. Added **per-account lockout** (`admins.fail_count`/`locked_until`, defaults `LOCK_THRESHOLD=8`/`LOCK_MINUTES=15`; defensive ALTER-TABLE migration in db.js). Aaron set his own strong passwords in server/.env (`ADMIN1_PASS`/`ADMIN2_PASS` — values NOT stored here) and removed `DEV_SHOW_CODE`, so the site is currently fully locked: **to actually log in, SMTP must be configured** (`SMTP_USER`=gmail + `SMTP_PASS`=Google App Password, `smtp.gmail.com:465`) so codes reach their inboxes — OR add `DEV_SHOW_CODE=1` for localhost testing. Verified: happy-path 6/6 (dev flag on) + fail-closed 4/4 (flag off). Still TODO for prod: HTTPS + `BEHIND_TLS=true` (Secure cookies). `auth.ensureAdminSeed`/`checkAdminPasscode` are dead code.

**Going LIVE (in progress 2026-07-06):** Aaron wants OUT of demo mode; withdraw target confirmed `p2key1@gmail.com`. Created `server/.env` (gitignored) with `PAYOUT_EMAIL=p2key1@gmail.com`, `PAYPAL_ENV=production`, a strong admin passcode `[redacted]` (replaces legacy [redacted] — but only seeds on a FRESH db; existing `server/data/p2k.db` still holds the [redacted] hash, so clear `server/data/` + restart to apply). **BLOCKED on Aaron providing real PayPal `PAYPAL_CLIENT_ID`+`PAYPAL_SECRET`** (server stays DEMO until both set). Flagged 3 live-money risks to him: B2 same-account payout (keys' account vs p2key1 — can't pay an account to itself), B1 withdrawable balance includes projected listen-royalty not real money (offered to gate withdrawable=verified sales only), R4 incoming payment capture needs PayPal JS SDK wiring to truly verify. Awaiting his creds + answers.

**Ticketing (added 2026-07-03):** admin "Announce a Show" creates events (auto-posts a News item); fans buy via the same PayPal `_xclick` flow and a digital ticket is generated **client-side on the spot** — unique code `P2K-XXX-######` + QR (qrcodejs CDN) encoding `https://p2k-music.ca/t/<code>` — saved to a "My Tickets" IndexedDB store. New IndexedDB stores `events` + `tickets` (DB_VERSION bumped 3→4).

Also added 2026-07-03: an admin **Scan & Check-In** door mode (`#checkin` section, admin-gated) — camera QR scan via jsQR (jsDelivr CDN; cdnjs path was ORB-blocked) + manual code entry; validates against the LOCAL `tickets` store and sets `status:'checked-in'` + `checkedInAt`. And **Merch now sells** via the same PayPal `_xclick` flow (`MERCH_ITEMS` map, size + quantity → checkout modal).

Backend TODO: verify PayPal payments (tickets + $16 songs + merch); server ISSUE/sign tickets (client generation is forgeable); door-validation endpoint so check-in works cross-device (the door scanner currently only sees tickets in ITS OWN browser) — e.g. `GET /api/ticket/:code` returns/validates + marks used.

**AdSense (2026-07-03):** publisher `ca-pub-2580922665149434`. Fixed P2K's broken `data-ad-slot="auto"` → placeholder slot IDs he must replace with real numeric ones from the dashboard; added `ads.txt` (root) + `ADSENSE-SETUP.md` guide + an initializer that only pushes ad units with numeric slots. Ads won't show until Google approves the live domain.

**Cookie consent (2026-07-03):** banner (`#cookieBanner`, localStorage `p2kCookieConsent`) gates ads — `initAds()` only runs after Accept; "Cookie Settings" link in footer reopens it. For strict EEA legal compliance, enable Google's certified CMP (AdSense ▸ Privacy & messaging) — noted to Aaron.

**Podcast (2026-07-03):** `#podcast` section — admin publishes episodes (upload or URL → `podcast` store, auto-posts to News), invites guests (generates `GUEST-XXXXX` code + copyable message, saved to `guests` store), and fans apply via a public form (also `guests` store, `type:'application'` vs `'invite'`). Episodes play via a dedicated `podcastAudio` element. **DB_VERSION now 5** (added `podcast` + `guests` stores).

**Revenue & Profit dashboard (2026-07-03):** admin-only `#profit` section monitors all site profit in one place. `logRevenue(source,label,amount)` auto-banks every sale — hooked into `confirmPurchase` (music), `confirmTicketPurchase` (ticket), merch pay-click (`recordMerchIntent`); listening usage banks per-minute via the wallet tick into `listenBanked` (persisted in `settings`); `recordIncome()` lets P2K log ad payouts/bookings manually. Ledger lives in the `revenue` store (**DB_VERSION now 6**). Dashboard shows total, per-source cards + bar chart + transaction feed. NOTE: figures are self-reported/client-side — the backend must reconcile against verified PayPal payments + real AdSense earnings to be the true source of truth (money currently only *moves* through PayPal, not tracked server-side).

**⚠️ Rights caution (raise for any monetization work):** several catalog tracks look like OTHER artists'/remixes (e.g. "Ketsa - Owned the Day", "DJ Meemx …", "dannyminus … Remix"). Selling them for $16 + running ads needs P2K to actually own/license them — flagged to Aaron 2026-07-03. Relevant to tomorrow's payment backend. See [[aaron-working-style]].
