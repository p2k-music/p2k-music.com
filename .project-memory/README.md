# .project-memory — session memory backup

A committed snapshot of the working memory Claude has kept while building this project,
included so **none of the context/progress is lost** even though the live memory lives
outside the repo (in Claude's per-project store on the working machine).

- **`p2k-handoff.md`** — start here: current state + next steps.
- **`p2k-site-project.md`** — the detailed, chronological build log of everything.
- **`p2k-git-ownership.md`** — commit/push as P2K, never Aaron.
- **`aaron-working-style.md`** — working preferences.
- **`MEMORY.md`** — the index.

Notes:
- This is a **backup/reference snapshot**, not live memory — it may lag the working copy.
  The authoritative human/AI docs are [`../PROJECT-OVERVIEW.md`](../PROJECT-OVERVIEW.md)
  and [`../CLAUDE.md`](../CLAUDE.md).
- Obsolete/one-time credentials have been redacted; no current passwords or secrets are
  stored here.
- The backend's static server refuses to serve this folder and any `.md` file, so these
  notes are never exposed over HTTP.
